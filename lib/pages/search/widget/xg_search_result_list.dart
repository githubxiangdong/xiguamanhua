import 'package:flutter/material.dart';

class XGSearchResultList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('我是搜索结果'),
    );
  }
}
